<odoo>
    <data noupdate="1">
        <!-- Record rule definition -->
        <record id="remove_bg_model_rule" model="ir.rule">
            <field name="name">Remove Background Model Rule</field>
            <field name="model_id" ref="model_remove_bg_model"/>
            <field name="domain_force">[('user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        </record>
    </data>
</odoo>
